<h2>Sortie du <%= @trip.start_date %> au <%= @trip.end_date %></h2>
<%= link_to('Participer', new_trip_message_path(@trip)) %>


<h4>Other participant messages</h4>
<ul>
<% @trip.messages.each do |message| %>
  <li><%= message.content %> - <%= message.user.user_name %></li>
<% end %>
</ul>
